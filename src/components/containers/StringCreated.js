import React, {useContext} from 'react'

const StringCreated = () => {
    const { string, setString } = useContext(StringContext)

    const content = ()

    return content
}

export default StringCreated